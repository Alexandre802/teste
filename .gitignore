node_modules/
build/

# Scratch renders and stills. Deliverables that should survive the working
# copy go in dist/ instead, which IS versioned.
out/

.remotion/
*.log
.DS_Store
.env
.env.local

# Audio intermediates — tools/build-audio.sh converts these to mp3,
# and only the mp3s are committed.
public/audio/*.wav
public/audio/sfx/*.wav
