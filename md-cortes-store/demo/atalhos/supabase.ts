"use client";

/**
 * Substitui `lib/supabase/client` na demonstração.
 *
 * Devolver `null` é o mesmo caminho que o app já segue quando não há chave
 * configurada: tudo funciona no espelho local e nada é enviado para fora.
 * Assim o pacote do Supabase nem entra no arquivo da demonstração.
 */
export function getSupabase(): null {
  return null;
}
